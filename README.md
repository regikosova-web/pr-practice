# pr-practice

A tiny practice project for learning the pull request workflow with Claude Code.

## What this is

This repo exists so you can practice the full loop: make a change, commit it,
push a branch, and open a pull request on GitHub.

## Files

- `greet.py` — a small script with one function, `greet(name)`.
